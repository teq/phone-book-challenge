const chai = require('chai');
const faker = require('faker');

const { request, authorize } = require('../helpers');

const { expect } = chai;

describe('phone number create', () => {});

describe('phone number delete', () => {});

describe('phone number update', () => {});

describe('phone number list', () => {});
